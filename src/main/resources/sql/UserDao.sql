CREATE TABLE user (
  id     INT          NOT NULL AUTO_INCREMENT,
  u_name VARCHAR(100) NOT NULL,
  age    INT          NOT NULL,
  PRIMARY KEY (id)
);

# INSERT INTO user (u_name, age) VALUES ("zou22", 22)